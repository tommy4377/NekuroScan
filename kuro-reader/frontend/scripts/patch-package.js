#!/usr/bin/env node
// Stub per patch-package - non fa nulla
// Questo previene errori quando rollup cerca di eseguire patch-package
console.log('patch-package: no patches to apply');
process.exit(0);

